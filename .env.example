# RefreshArr Environment Configuration
# Copy this file to .env and update with your values

# Sonarr Configuration (optional - configure if you want to clean up TV shows)
SONARR_URL=http://127.0.0.1:8989
SONARR_API_KEY=your-sonarr-api-key-here

# Radarr Configuration (optional - configure if you want to clean up movies)
RADARR_URL=http://127.0.0.1:7878
RADARR_API_KEY=your-radarr-api-key-here

# Request Settings
REQUEST_TIMEOUT=30s          # Timeout for HTTP requests
REQUEST_DELAY=500ms          # Delay between API calls to be nice to the server
CONCURRENT_LIMIT=5           # Maximum concurrent operations

# Logging
LOG_LEVEL=INFO               # DEBUG, INFO, WARN, ERROR

# Operation Mode
DRY_RUN=false                # Set to true to preview changes without making them

# Example Usage:
# 1. Copy this file: cp .env.example .env
# 2. Update API keys with your actual values (at least one service required)
# 3. Adjust other settings as needed
# 4. Run: ./refresharr or ./refresharr-cli
#
# Service Selection:
# - Configure both services to run cleanup for both TV shows and movies
# - Configure only Sonarr to clean up TV shows only
# - Configure only Radarr to clean up movies only
# - Use --service flag to override: --service sonarr, --service radarr, or --service both
